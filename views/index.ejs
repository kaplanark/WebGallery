<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=s, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="css/bootstrap.min.css">
    <link rel="stylesheet" href="css/lightbox.min.css">
    <title>Galery</title>
    <style>
        .body {
            height: 100vh;
            width: 100%;
        }
        .galery{
        }
        nav{
            background: #333;
            padding: 12px;
            position: fixed;
            display: flex;
            justify-content: space-between;
            width: 100%;
            bottom: 0;
            left: 0;
        }
        .toast-container {
            position: fixed;
            right: 10px;
            top: 10px;
        }

        .col-lg-6 {
            margin-top: 15rem;
        }
    </style>
</head>

<body>
    <div class="container">
        <section>
            <div class="galery">
                <div class="row">
                    <div class="col-md-3">
                        <div class="img">
                            <a class="" href="http://lokeshdhakar.com/projects/lightbox2/images/image-1.jpg" data-lightbox="example-1">
                                <img class="img-fluid" src="http://lokeshdhakar.com/projects/lightbox2/images/thumb-1.jpg" alt="image-1" />
                            </a>  
                        </div>
                    </div>
                </div>
            </div>
        </section>
        <nav class="shadow">
            <form action="/uploadphoto" enctype="multipart/form-data" method="post">
                <div class="input-group">
                    <input type="file" class="form-control" name="photo" accept="image/*">
                    <input type="submit" class="btn btn-primary"></input>
                </div>
            </form>
            <div class="btn-group" role="group">
                <button class="btn btn-success">İndir</button>
                <button class="btn btn-danger">Sil</button>
            </div>
        </nav>
    </div>
    <div class="toast-container">
        <% if (message && message.length> 0) { %>
            <div class="toast fade show " role="alert" aria-live="assertive" aria-atomic="true">
                <div class="toast-header">
                    <strong class="me-auto text-success">Bildirim</strong>
                    <small>
                        <%= message.length %>
                    </small>
                    <button type="button" class="btn-close" data-bs-dismiss="toast"
                        aria-label="Close"></button>
                </div>
                <div class="toast-body">
                    <%= message %> başarılı bir şekilde yüklendi.
                </div>
            </div>
        <% } if(errors && errors.length>0) {%>
            <div class="toast fade show " role="alert" aria-live="assertive" aria-atomic="true">
                <div class="toast-header">
                    <strong class="me-auto text-warning">Hata</strong>
                    <small>
                        <%= erros.length %>
                    </small>
                    <button type="button" class="btn-close" data-bs-dismiss="toast"aria-label="Close"></button>
                </div>
                <div class="toast-body">
                    <%= errors %>
                </div>
            </div>
        <% } %>
    </div>
    <script src="js/jquery.min.js"></script>
    <script src="js/bootstrap.min.js"></script>
    <script src="js/lightbox.min.js"></script>
    <script>
        //from imput value control
        $(document).ready(function () {
            $('input:submit').attr('disabled', true);
            $('input:file').change(function () {
                if ($(this).val()) {
                    $('input:submit').removeAttr('disabled');
                }
                else {
                    $('input:submit').attr('disabled', true);
                }
            });
        });
        //function that automatically turns off notifications
        $(document).ready(function () {
            window.setTimeout(function () {
                $(".toast").alert('close');
            }, 4000);
        });
    </script>
</body>

</html>